The result of the density ratio model $\omega^{(m)}(\mathbf{Z},\xi^{(m)}),m=1,2$ is stored in file 'density_result.csv'. The first column represents the parameter $\xi^{(1)}$ while the second one represents $\xi^{(2)}$.
